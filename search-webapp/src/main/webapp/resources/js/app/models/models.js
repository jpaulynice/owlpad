define([
        'jquery',
        'underscore',
        'backbone',
        'marionette'
    ], function($,_,Backbone,Marionette) {

	var searchModel = Backbone.Model.extend({
		
	});
    
    return searchModel;
});